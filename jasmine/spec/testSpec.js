// describe("calculatenNextCellState function", function() {
//     it("determines if in the next generation the cell will be alive or dead", function() {
//         expect(calculateNextCellState(3)).toEqual(6);
//     });
// });

describe("calculateLiveNeighbors function", function() {

    beforeEach(function() {
        // game  = new GameOfLife(3,3);
    });

    it("calculates the alive neighbors of a cell", function() {
        expect(true).toBe(true);
        // let testArray =[[0,1,0],
        //                 [0,1,0],
        //                 [0,1,0]];
        // expect(game.calculateLiveNeighbors(testArray[1][1])).toEqual(2);
    });
});
